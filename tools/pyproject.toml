[project]
name = "f1-tools"
version = "0.1.0"
description = "F1 data fetching tools for f1-purple-line"
requires-python = ">=3.10"
dependencies = [
    "fastf1>=3.0.0",
    "pandas>=2.0.0",
]

[tool.uv]
dev-dependencies = []
